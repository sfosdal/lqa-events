# Generated by scripts/fetch-events.mjs (CI builds them fresh; local runs are for preview)
site/events.json
site/events.ics
site/events-*.ics
site/team-*.ics
site/teams.ics
.DS_Store
# local Ticketmaster key for preview fetches — never committed
.env
site/teams.json
site/marks/opp/
# the archive record the build writes for the `archive` branch (the workflow commits it there)
events-archive.json
